# 热模型记录评估

**同一记录的离线评估，不是硬件验证或补测收益证明。**

训练 100 行，后续检验 499 行；仅首行实测温度用于初始化。

## 参数能相信到什么程度

|参数|值|来源|局部RMS灵敏度 °C/范围|边界标记|
|---|---:|---|---:|---:|
|U|2|单初值拟合，精度未确立|2.3981|-1|
|alpha1|0.005|单初值拟合，精度未确立|22.4155|-1|
|alpha2|0.0075|默认假设，未估计|0|0|

U单位为W/(m² K)，alpha单位为W/%；边界标记-1/0/1分别表示下界/未标记/上界。
局部数值秩：2/3；步长减半的相对矩阵差：1.8749645405705885e-06。
最弱方向（U、alpha1、alpha2的范围缩放坐标）：[0.0, 0.0, 1.0]。
它不是置信区间；边界上的方向不保证双向可行。灵敏度列余弦不是参数的统计相关系数。
- U / alpha1的灵敏度列余弦：-0.793795。绝对值接近1表示局部输出变化形状相似，不是不可辨识的单独证明。
- U / alpha2的灵敏度列余弦：未定义（存在零灵敏度列）。绝对值接近1表示局部输出变化形状相似，不是不可辨识的单独证明。
- alpha1 / alpha2的灵敏度列余弦：未定义（存在零灵敏度列）。绝对值接近1表示局部输出变化形状相似，不是不可辨识的单独证明。

## 冻结参数后的预测

训练RMSE：1.057580°C；后续RMSE：13.556313°C。
后续每通道平均残差（预测−测量）：[0.9856152188545098, 17.091618278529282]°C；不是零偏估计。
没有预先给定的工程容差，不能把这些数字转为合格/不合格。后段实测没有参与拟合或灵敏度计算。

未激励增益的外加假设敏感性（其他参数固定，不是联合置信区间）：
- alpha2=0.002：训练最大预测差0°C，后续RMSE 2.868108°C。
- alpha2=0.015：训练最大预测差0°C，后续RMSE 32.812469°C。

## 下一步

- alpha2只保留默认假设：需要该路授权输入生效后的温度观测，或独立增益标定。
- 存在触及参数边界的拟合量；先审查边界依据、模型和测量，不据此宣称设备故障或直接放宽边界。
- 局部灵敏度存在数值退化；检查最弱参数组合及缺失激励，不把优化器成功当成三个参数均可估计。
- 结合最弱参数方向、灵敏度列相似性、独立噪声/参考证据讨论补测；没有完整允许候选和成本，不给最优试验排名。
- 执行前仍须确认模型/初态、输入单位与时序、允许幅值/变化率、温度限制、观测时长及其他通道控制。

假设：固定23°C环境、作者双节点几何/热容量/辐射设定和参数范围；不是任意设备CSV的通用模型。
**不支持工程放行；没有设备动作、最优补测排序或AI性能优势结论。**
