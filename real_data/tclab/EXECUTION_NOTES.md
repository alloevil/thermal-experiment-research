# Execution notes and failed environment attempt

- Initial system Python had NumPy 2.5.3 but an incompatible older SciPy/Pandas binary combination. Seven small data/model tests passed with a SciPy version warning; formal execution stopped importing Pandas before any fitting occurred (`numpy.dtype size changed`). The failed output directory is `results/first-run`; its environment receipt is retained. Failure console output is retained in `logs/failed-system-environment.log`.
- No global package was upgraded or changed. A new ignored `.venv-tclab` was created using the pinned `requirements.in` / hash-bearing `requirements.lock`. It contains 13 packages including NumPy 2.5.3, SciPy 1.18.1, Pandas 3.0.6 and Matplotlib 3.11.2.
- The successful main run is `results/locked-run`. The original `protocol.md` was frozen by its hash at the beginning of that run. An environment explanation was briefly appended while the computation was in progress; it was moved to this separate execution note afterward, restoring the exact protocol bytes checked at startup. No numerical setting, split, objective, initial value, bound or fit was changed; the saved protocol digest matches the file.
- The source script was executed unchanged through `runpy` with the Agg backend. Its `import pandas` is retained even though its own fitting code uses NumPy. Its full-data fit is an implementation reproduction, not a temporal holdout experiment.
- No model APIs, remote optimization services or hardware-control scripts were executed. Source acquisition used GitHub's fixed-commit API after a raw-file download timed out; no data were synthesized to replace the missing response.
- All new successful and failed outputs are separate from archived studies. No automatic commit, push, remote metadata change or release was performed for this study.
